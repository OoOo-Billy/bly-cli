"use strict"

// const chalk = require('chalk');
// const fs = require('fs');
// const path = require('path');
// require('shelljs/global');

// const log = function(...txt) {
//   console.log(chalk.magenta(...txt))
// }

function create(projectName, templateType) {
  console.log('createProject beginning...');
  console.log('projectName:', projectName);
  console.log('templateType:', templateType);
  // log('createProject beginning...');
  // log('projectName:', projectName);
  // log('templateType:', templateType);
  // var p = process.cwd(); /** 命令行运行页面路径 */
  // cd(p)
}

module.exports = create